pipeline {
    agent { 
	docker { 
		image 'alpine:latest'
		}
		}
    stages {
    	stage('data') {
			script{
				sh 'touch "test_$(date +"%F %T")"'
				sh 'sleep 10s'
			}
			}
			
		}
	}
    stages {
    }
}
