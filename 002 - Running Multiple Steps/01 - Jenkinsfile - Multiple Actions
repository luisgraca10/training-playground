pipeline {
    agent { 
	docker { 
		image 'alpine:latest'
		}
		}
    stages {
    	stage('data') {
		steps {
			script{
				sh 'touch "test_$(date +"%F %T")"'
				sh 'sleep 10s'
			}
		}	
	}
			
    }
}
