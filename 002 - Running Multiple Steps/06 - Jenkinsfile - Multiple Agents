pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('Maven Version') {
            steps {
	    	step{
			script{
				def pomv = "$POM_VERSION"
			}
			
		}
	    	
                sh 'mvn --version'
            }
        }
    }
}
