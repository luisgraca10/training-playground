pipeline {
    agent none
    stages {
        stage('Maven Version') {
                sh 'mvn --version'
        }
	stage('NPM Version') {
                sh 'npm --version'
        }
    }
}
