pipeline {
    agent none
    stages {
        stage('Maven Version') {
		steps{
			sh 'mvn --version'
		}
                
        }
	stage('NPM Version') {
                steps{
			sh 'npm --version'
		}
        }
    }
}
