pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    tools {
        maven 'apache-maven-3.0.1' 
    }	
    stages {
        stage('Maven Version') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}
