pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('Maven Version') {
            steps {
                sh 'mvn --version'
            }
        }
    }
}
