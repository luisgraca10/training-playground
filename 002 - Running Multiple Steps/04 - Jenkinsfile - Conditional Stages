pipeline {
    parameters{
    	choice(NAME, choices: ['build', 'teste'], description: 'Qual o stage pá?')
    }
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage('"$(params.CHOICE)"')
	 steps {
	  sh 'echo build'	
	 }
	 stage('"$(params.CHOICE)"') {
	 	steps {
	 	sh 'echo teste'
	}
    }
}
