pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage('build'){
		sh 'date > currDate.txt'
	}
	stage('test'){
		sh 'grep -i "*2019*" currDate.txt'
	}
	}
    }
}
