pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage('build'){
		def saveAndPrintDate(filename = '') {
   		sh "date > $filename && cat $filename"
		}
	}
    }
}
