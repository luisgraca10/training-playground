pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stage {
    	steps ('build'){
		sh 'date > currDate.txt'
	}
	steps ('test'){
		sh 'grep -i "*2023*" currDate.txt'
	}
    }
}
