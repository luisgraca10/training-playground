pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	steps ('build'){
		sh 'date > currDate.txt'
	}
	steps ('test'){
		sh 'grep -i "*2019*" currDate.txt'
	}
    }
}
